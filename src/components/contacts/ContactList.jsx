import ContactCard from './ContactCard'

function ContactList() {
  return (
    <section className="list">
      <ContactCard name="Contact placeholder" channel="Channel placeholder" />
    </section>
  )
}

export default ContactList
